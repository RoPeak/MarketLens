name: marketlens
version: "1.0.0"
config-version: 2

profile: marketlens

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  marketlens:
    silver:
      +materialized: view
      +schema: main
    gold:
      +materialized: table
      +schema: main
